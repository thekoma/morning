apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "morning.fullname" . }}-test-connection"
  labels:
    {{- include "morning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "morning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
